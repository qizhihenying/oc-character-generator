# 卡片下载功能 - 使用说明

## 📦 安装依赖

在项目根目录运行：

```bash
npm install
```

这会自动安装以下新增依赖：
- `html2canvas`: ^1.4.1 - 用于将HTML元素转换为图片
- `@types/html2canvas`: ^1.0.0 - TypeScript类型定义

## 🎨 功能说明

### 核心功能
- ✅ 将角色卡片导出为高清PNG图片
- ✅ 自动生成文件名（角色名_稀有度_时间戳.png）
- ✅ 2倍清晰度渲染（scale: 2）
- ✅ 支持跨域图片（如果有MJ生成的图片）
- ✅ 友好的加载和成功提示

### 卡片内容
下载的卡片图片包含：
- 稀有度标识（星级）
- 角色图片（如果已生成）
- 角色名称
- 基础属性（性别、年龄、色调）
- 详细特征（发型、眼睛、服装、配饰、风格、氛围）
- 水印（OC人设盲盒 + 生成日期）

### 不包含的内容
- 操作按钮（复制、收藏、下载、AI绘图）
- 这些按钮在卡片外部，不会被下载到图片中

## 🔧 使用方法

### 1. 基础使用

```typescript
import CharacterCardDownload from './components/CharacterCardDownload';

// 准备角色数据
const character: CharacterCard = {
  id: '001',
  name: '银月',
  rarity: 'SSR',
  gender: 'Female',
  age: 'Teen',
  bodyType: 'Slender',
  hairStyle: 'silver long flowing hair with blue highlights',
  eyeColor: 'glowing purple eyes',
  outfit: 'gothic lolita dress with lace details',
  accessories: 'cross accessories and chains',
  artStyle: 'Anime · Watercolor',
  atmosphere: 'Mysterious · Elegant',
  colorTone: 'Cool',
  fullPrompt: '完整的提示词...',
  createdAt: Date.now(),
  isFavorite: false,
  imageUrl: 'https://...' // 可选
};

// 使用组件
<CharacterCardDownload
  character={character}
  onCopy={() => console.log('复制提示词')}
  onFavorite={() => console.log('收藏')}
  onDraw={() => console.log('AI绘图')}
/>
```

### 2. 集成到现有代码

在你的 `App.tsx` 或相关组件中：

```typescript
import { useState } from 'react';
import CharacterCardDownload from './components/CharacterCardDownload';

function App() {
  const [currentCard, setCurrentCard] = useState<CharacterCard | null>(null);
  
  // 生成角色时创建卡片数据
  const handleGenerate = () => {
    const prompt = generatePrompt(); // 你现有的生成逻辑
    
    // 从提示词中提取信息创建卡片
    const card: CharacterCard = {
      id: generateId(),
      name: generateCharacterName(), // 自动生成名字
      rarity: calculateRarity(), // 计算稀有度
      gender: extractGender(prompt),
      age: config.age || 'Unknown',
      bodyType: config.bodyType || 'Unknown',
      hairStyle: extractHairStyle(prompt),
      eyeColor: extractEyeColor(prompt),
      outfit: extractOutfit(prompt),
      accessories: extractAccessories(prompt),
      artStyle: extractArtStyle(prompt),
      atmosphere: config.atmosphere || 'Unknown',
      colorTone: config.colorTone || 'Unknown',
      fullPrompt: prompt,
      createdAt: Date.now(),
      isFavorite: false
    };
    
    setCurrentCard(card);
  };
  
  return (
    <div>
      {currentCard && (
        <CharacterCardDownload
          character={currentCard}
          onCopy={() => navigator.clipboard.writeText(currentCard.fullPrompt)}
          onFavorite={() => toggleFavorite(currentCard.id)}
          onDraw={() => submitToMidjourney(currentCard.fullPrompt)}
        />
      )}
    </div>
  );
}
```

## 🎯 提取信息的辅助函数

为了从提示词中提取信息，你可以创建这些辅助函数：

```typescript
// src/utils/cardDataExtractor.ts

export function extractGender(prompt: string): string {
  if (prompt.toLowerCase().includes('male') && !prompt.toLowerCase().includes('female')) {
    return 'Male';
  }
  if (prompt.toLowerCase().includes('female')) {
    return 'Female';
  }
  return 'Unknown';
}

export function extractHairStyle(prompt: string): string {
  // 从提示词中提取发型描述
  const hairMatch = prompt.match(/(\w+\s+\w+\s+hair[^,]*)/i);
  return hairMatch ? hairMatch[1] : 'Unknown';
}

export function extractEyeColor(prompt: string): string {
  // 从提示词中提取眼睛描述
  const eyeMatch = prompt.match(/(\w+\s+\w+\s+eyes[^,]*)/i);
  return eyeMatch ? eyeMatch[1] : 'Unknown';
}

export function extractOutfit(prompt: string): string {
  // 从提示词中提取服装描述
  const outfitMatch = prompt.match(/wearing\s+([^,]+)/i);
  return outfitMatch ? outfitMatch[1] : 'Unknown';
}

export function extractAccessories(prompt: string): string {
  // 提取配饰信息
  const accessories: string[] = [];
  if (prompt.includes('cross')) accessories.push('cross');
  if (prompt.includes('chains')) accessories.push('chains');
  if (prompt.includes('wings')) accessories.push('wings');
  return accessories.join(', ') || 'None';
}

export function extractArtStyle(prompt: string): string {
  // 提取艺术风格
  const styles: string[] = [];
  if (prompt.toLowerCase().includes('anime')) styles.push('Anime');
  if (prompt.toLowerCase().includes('watercolor')) styles.push('Watercolor');
  if (prompt.toLowerCase().includes('oil painting')) styles.push('Oil Painting');
  return styles.join(' · ') || 'Unknown';
}

export function generateCharacterName(): string {
  // 随机生成角色名字
  const firstNames = ['银月', '星辰', '暗夜', '晨曦', '幻影', '冰霜', '烈焰', '翡翠'];
  const lastNames = ['之', '的', '·'];
  const suffixes = ['守护者', '行者', '使者', '骑士', '法师', '游侠'];
  
  const first = firstNames[Math.floor(Math.random() * firstNames.length)];
  const suffix = suffixes[Math.floor(Math.random() * suffixes.length)];
  
  return `${first}${suffix}`;
}

export function calculateRarity(prompt: string): 'N' | 'R' | 'SR' | 'SSR' | 'UR' {
  let score = 0;
  
  // 特殊元素加分
  if (prompt.includes('mechanical wings')) score += 20;
  if (prompt.includes('heterochromia')) score += 15;
  if (prompt.includes('holographic')) score += 10;
  if (prompt.includes('glowing')) score += 8;
  if (prompt.includes('magical')) score += 8;
  if (prompt.includes('cybernetic')) score += 10;
  
  // 根据分数判定
  if (score >= 40) return 'UR';
  if (score >= 25) return 'SSR';
  if (score >= 15) return 'SR';
  if (score >= 8) return 'R';
  return 'N';
}
```

## 🎨 自定义样式

你可以修改 `CharacterCardDownload.tsx` 中的样式：

### 修改卡片尺寸
```typescript
// 当前: w-80 h-[600px]
// 可改为: w-96 h-[700px] (更大)
// 或: w-72 h-[500px] (更小)
```

### 修改稀有度颜色
```typescript
const rarityColors = {
  'N': 'from-gray-400 to-gray-600',
  'R': 'from-blue-400 to-blue-600',
  'SR': 'from-purple-400 to-purple-600',
  'SSR': 'from-yellow-400 to-yellow-600',
  'UR': 'from-pink-400 via-purple-500 to-cyan-500' // 彩虹渐变
};
```

### 修改图片清晰度
```typescript
// 在 html2canvas 配置中
scale: 2, // 改为 3 或 4 可以更清晰，但文件更大
```

## 📱 移动端适配

组件已经是响应式的，但如果需要针对移动端优化：

```typescript
// 根据屏幕宽度调整卡片大小
const isMobile = window.innerWidth < 768;
const cardClass = isMobile ? 'w-72 h-[500px]' : 'w-80 h-[600px]';
```

## 🔍 常见问题

### Q: 下载的图片模糊怎么办？
A: 增加 `scale` 参数：
```typescript
scale: 3, // 或更高
```

### Q: 跨域图片无法下载？
A: 确保图片服务器支持CORS，或使用代理。已设置 `useCORS: true`。

### Q: 下载速度慢？
A: 这是正常的，html2canvas 需要时间渲染。可以添加加载动画。

### Q: 想修改文件名格式？
A: 修改这行代码：
```typescript
const fileName = `OC_${character.name}_${character.rarity}_${Date.now()}.png`;
```

### Q: 能下载为JPG格式吗？
A: 可以，修改：
```typescript
canvas.toBlob((blob: Blob | null) => {
  // ...
}, 'image/jpeg', 0.95); // 第二个参数是格式，第三个是质量
```

## 🚀 进阶功能

### 1. 批量下载
```typescript
const downloadMultipleCards = async (cards: CharacterCard[]) => {
  for (const card of cards) {
    await downloadCard(card);
    await new Promise(resolve => setTimeout(resolve, 1000)); // 延迟1秒
  }
};
```

### 2. 添加水印
在卡片底部已经有了水印，你可以自定义：
```typescript
<div className="mt-4 text-center text-xs text-gray-400">
  OC人设盲盒 · {new Date(character.createdAt).toLocaleDateString()}
  · ID: {character.id}
</div>
```

### 3. 分享到社交媒体
```typescript
const shareCard = async (blob: Blob) => {
  if (navigator.share) {
    const file = new File([blob], 'character.png', { type: 'image/png' });
    await navigator.share({
      files: [file],
      title: '我的OC角色',
      text: '看看我抽到的角色！'
    });
  }
};
```

## 📝 注意事项

1. **首次使用需要安装依赖**: `npm install`
2. **TypeScript错误**: 安装后重启IDE/编辑器
3. **图片跨域**: 如果MJ图片有跨域问题，需要配置CORS
4. **文件大小**: 高清图片可能较大（500KB-2MB）
5. **浏览器兼容**: 现代浏览器都支持，IE不支持

## 🎉 完成！

现在你可以：
1. 运行 `npm install` 安装依赖
2. 在你的代码中导入 `CharacterCardDownload` 组件
3. 传入角色数据
4. 点击"下载卡片"按钮即可保存精美的角色卡片！

---

**提示**: 如果遇到问题，检查浏览器控制台的错误信息。
