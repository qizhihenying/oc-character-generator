# 人物一致性功能指南 v1.2.0

## 🎯 什么是人物一致性？

人物一致性是指在多次生成中保持角色的核心特征（如发型、眼睛、服装等）不变，让同一个角色在不同场景、姿势、表情下都能被识别出来。

---

## 🔧 三种一致性模式

### 1. 种子模式（Seed Mode）

**原理：** 使用相同的随机种子生成风格相似的角色

**优点：**
- ✅ 简单易用，无需额外资源
- ✅ 完全离线，不依赖外部图片
- ✅ 适合快速迭代

**缺点：**
- ⚠️ 一致性较弱，只能保证风格相似
- ⚠️ 细节可能有较大差异

**适用场景：**
- 创建风格统一的角色系列
- 快速探索同一风格的不同角色
- 不需要严格一致性的创作

**使用方法：**
```
1. 启用人物一致性模式
2. 选择"种子模式"
3. 生成第一个角色（系统自动生成种子）
4. 继续生成，种子保持不变
5. 所有角色将具有相似的风格
```

---

### 2. 参考图模式（Character Reference Mode）

**原理：** 使用 MidJourney 的 `--cref` 参数引用角色图片

**优点：**
- ✅ 一致性强，能精确保持角色特征
- ✅ 支持表情、姿势、场景变化
- ✅ 官方支持，效果稳定

**缺点：**
- ⚠️ 需要先有一张角色参考图
- ⚠️ 仅支持 MidJourney v6+
- ⚠️ 需要图片URL（需上传到图床）

**适用场景：**
- 已有满意的角色图，需要生成更多姿势
- 创建角色的不同表情、动作
- 制作角色设定图、表情包

**使用方法：**
```
1. 先生成一个满意的角色图
2. 将图片上传到图床（如 Discord、Imgur）
3. 复制图片URL
4. 启用人物一致性模式
5. 选择"参考图模式"
6. 粘贴图片URL
7. 生成新图，角色将保持一致
```

**技术参数：**
```
--cref https://example.com/character.jpg
--cw 100
```
- `--cref`: 角色参考图URL
- `--cw 100`: 参考权重100（完全一致）

---

### 3. 组合模式（Combined Mode）

**原理：** 同时使用种子和参考图，双重保障

**优点：**
- ✅ 一致性最强
- ✅ 结合两种方法的优点
- ✅ 效果最稳定

**缺点：**
- ⚠️ 需要参考图URL
- ⚠️ 设置稍复杂

**适用场景：**
- 需要极高一致性的项目
- 商业创作、IP角色设计
- 系列作品创作

**使用方法：**
```
1. 启用人物一致性模式
2. 选择"组合模式"
3. 输入参考图URL
4. 系统自动使用固定种子
5. 生成的所有角色将高度一致
```

---

## 🔒 特征锁定功能

### 什么是特征锁定？

锁定特定特征后，该特征将在所有生成中保持不变，其他特征可以随机变化。

### 可锁定的特征

| 特征 | 说明 | 示例 |
|------|------|------|
| **发型发色** | 头发的颜色和样式 | 深紫色凌乱头发 |
| **眼睛特征** | 眼睛的颜色和形状 | 发光的紫色眼睛 |
| **服装主体** | 主要服装 | 黑色长款外套 |
| **配饰装备** | 配饰和装备 | 十字架饰品与链条 |

### 使用场景

#### 场景1：固定发型，变换服装
```
✅ 锁定：发型发色、眼睛特征
❌ 不锁定：服装主体、配饰装备

结果：同一个角色，不同服装造型
```

#### 场景2：固定服装，变换发型
```
✅ 锁定：服装主体、配饰装备
❌ 不锁定：发型发色、眼睛特征

结果：同一套服装，不同角色穿着
```

#### 场景3：完全锁定
```
✅ 锁定：全部特征

结果：完全相同的角色，只有姿势/表情变化
```

---

## 📋 实战案例

### 案例1：创建角色设定图

**目标：** 为同一个角色生成多个视角和姿势

**步骤：**
```
1. 生成第一个角色
   - 性别：女性
   - 风格：动漫
   - 许愿：樱花粉色长发，粉色眼睛

2. 保存生成的图片并上传到图床

3. 启用人物一致性模式
   - 模式：参考图模式
   - 参考图URL：粘贴图床链接
   - 锁定：全部特征

4. 修改许愿内容：
   - "正面特写"
   - "全身站立"
   - "侧面半身"
   - "背面视角"

5. 依次生成，获得完整的角色设定图
```

**效果：** 同一个角色的多个视角，完美一致

---

### 案例2：创建表情包

**目标：** 同一角色的不同表情

**步骤：**
```
1. 使用参考图模式 + 组合模式

2. 锁定全部特征

3. 在许愿中添加表情：
   - "开心的表情"
   - "生气的表情"
   - "哭泣的表情"
   - "惊讶的表情"
   - "害羞的表情"

4. 批量生成（可用5连抽）
```

**效果：** 同一角色的表情包素材

---

### 案例3：创建角色系列

**目标：** 风格统一的多个角色（如团队成员）

**步骤：**
```
1. 启用人物一致性模式
   - 模式：种子模式
   - 锁定：服装主体、配饰装备

2. 固定设置：
   - 风格：动漫
   - 服装：统一的团队制服

3. 变化设置：
   - 性别：依次选择男性/女性
   - 发型发色：自动随机
   - 眼睛特征：自动随机

4. 生成多个角色
```

**效果：** 穿着统一制服的团队成员，但每个人有独特外貌

---

### 案例4：角色成长系列

**目标：** 同一角色的不同年龄阶段

**步骤：**
```
1. 使用参考图模式

2. 锁定：发型发色、眼睛特征

3. 在许愿中添加年龄描述：
   - "10岁儿童版"
   - "16岁少年版"
   - "25岁成年版"
   - "40岁中年版"

4. 依次生成
```

**效果：** 同一角色的成长历程

---

## 💡 最佳实践

### 1. 选择合适的模式

| 需求 | 推荐模式 |
|------|----------|
| 快速探索 | 种子模式 |
| 高度一致 | 参考图模式 |
| 商业项目 | 组合模式 |
| 风格统一 | 种子模式 + 部分锁定 |

### 2. 特征锁定技巧

**全锁定：**
- 适合：表情包、设定图
- 只变化：姿势、表情、场景

**部分锁定：**
- 适合：角色系列、团队成员
- 保持：核心特征统一
- 变化：个性化特征

**不锁定：**
- 适合：风格探索
- 只保证：整体风格相似

### 3. 参考图使用技巧

**好的参考图：**
- ✅ 清晰的角色特征
- ✅ 正面或3/4侧面
- ✅ 完整的角色展示
- ✅ 背景简洁

**避免：**
- ❌ 模糊不清的图片
- ❌ 角色被遮挡
- ❌ 背景过于复杂
- ❌ 多个角色同框

### 4. 图床推荐

**免费图床：**
- Discord（上传到频道，右键复制链接）
- Imgur（https://imgur.com）
- PostImage（https://postimages.org）

**注意：**
- 确保图片链接可公开访问
- 建议使用永久链接
- 定期检查链接是否失效

---

## 🔍 常见问题

### Q1: 种子模式的一致性有多强？

**A:** 种子模式只能保证**风格相似**，不能保证细节一致。适合需要统一风格但不要求严格一致的场景。

### Q2: 参考图模式需要什么版本的 MidJourney？

**A:** 需要 MidJourney v6 或更高版本。niji 6 模式也支持。

### Q3: 可以使用本地图片吗？

**A:** 不可以。必须使用可公开访问的图片URL。需要先上传到图床。

### Q4: 锁定特征后还能修改吗？

**A:** 可以。取消勾选即可解锁。但已生成的角色不会改变。

### Q5: 组合模式比单独使用效果好多少？

**A:** 组合模式能提供最强的一致性保障，特别是在复杂场景下。建议商业项目使用。

### Q6: 为什么生成的角色还是不够一致？

**可能原因：**
1. 参考图质量不佳
2. 许愿内容与参考图冲突
3. 未锁定关键特征
4. 种子模式本身限制

**解决方案：**
- 使用更清晰的参考图
- 简化许愿内容
- 锁定更多特征
- 改用参考图模式或组合模式

---

## 📊 模式对比

| 特性 | 种子模式 | 参考图模式 | 组合模式 |
|------|----------|-----------|----------|
| **一致性** | ⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **易用性** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ |
| **速度** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **成本** | 免费 | 免费 | 免费 |
| **要求** | 无 | 需要参考图 | 需要参考图 |
| **适用场景** | 风格统一 | 角色一致 | 商业项目 |

---

## 🎨 创意应用

### 1. 漫画创作
- 使用参考图模式保持主角一致
- 为每个角色创建专属参考图
- 批量生成分镜

### 2. 游戏角色设计
- 组合模式创建主要角色
- 种子模式创建NPC系列
- 特征锁定创建职业变体

### 3. 小说插图
- 为主要角色使用参考图
- 为配角使用种子模式
- 保持整体风格统一

### 4. 品牌IP
- 组合模式确保IP形象一致
- 创建完整的角色设定图
- 制作表情包和周边素材

---

## 🚀 开始使用

1. **打开应用**
2. **找到"人物一致性模式"面板**
3. **勾选"启用"**
4. **选择合适的模式**
5. **配置锁定特征**
6. **开始生成！**

---

**祝你创作愉快！** 🎉✨

有任何问题欢迎反馈！
