# 🌐 绑定自定义域名教程

## 为什么需要绑定域名？

**问题：**
- Vercel 默认域名（`.vercel.app`）在国内需要代理才能访问
- 访问速度慢，用户体验差

**解决方案：**
- 绑定自己的域名
- 国内可以直接访问
- 速度更快，更专业

---

## 📋 前提条件

你需要：
1. ✅ 一个已备案的域名（如果服务器在国内）
2. ✅ 或者一个未备案的域名（使用海外 DNS）
3. ✅ 域名的 DNS 管理权限

---

## 🚀 绑定步骤

### 步骤 1：在 Vercel 添加域名

#### 1.1 进入项目设置
1. 访问 Vercel 控制台：https://vercel.com
2. 选择你的项目 `oc-character-generator`
3. 点击 "Settings"（设置）
4. 点击 "Domains"（域名）

#### 1.2 添加域名
1. 在输入框中输入你的域名，例如：
   - `oc.yourdomain.com`（推荐，使用子域名）
   - 或 `yourdomain.com`（使用主域名）
2. 点击 "Add"（添加）

#### 1.3 获取 DNS 配置信息
Vercel 会显示需要配置的 DNS 记录，通常是：
```
类型: CNAME
名称: oc（或 @）
值: cname.vercel-dns.com
```

---

### 步骤 2：配置 DNS

根据你的域名注册商，配置方式略有不同：

#### 方案 A：阿里云（万网）

1. **登录阿里云控制台**
   - 访问：https://dns.console.aliyun.com

2. **进入域名解析**
   - 找到你的域名
   - 点击 "解析设置"

3. **添加记录**
   - 记录类型：`CNAME`
   - 主机记录：`oc`（或你想要的子域名）
   - 记录值：`cname.vercel-dns.com`
   - TTL：`10分钟`（默认）
   - 点击 "确认"

#### 方案 B：腾讯云 DNSPod

1. **登录 DNSPod**
   - 访问：https://console.dnspod.cn

2. **选择域名**
   - 点击你的域名

3. **添加记录**
   - 记录类型：`CNAME`
   - 主机记录：`oc`
   - 记录值：`cname.vercel-dns.com`
   - TTL：`600`
   - 点击 "保存"

#### 方案 C：Cloudflare（推荐）

1. **登录 Cloudflare**
   - 访问：https://dash.cloudflare.com

2. **选择域名**
   - 点击你的域名

3. **添加 DNS 记录**
   - Type：`CNAME`
   - Name：`oc`
   - Target：`cname.vercel-dns.com`
   - Proxy status：`Proxied`（橙色云朵，推荐）
   - 点击 "Save"

**Cloudflare 优势：**
- ✅ 免费 CDN 加速
- ✅ 国内访问更快
- ✅ 自动 HTTPS
- ✅ DDoS 防护

#### 方案 D：其他域名商

通用步骤：
1. 登录域名管理后台
2. 找到 DNS 解析/域名解析
3. 添加 CNAME 记录：
   - 主机记录/名称：`oc`
   - 记录类型：`CNAME`
   - 记录值/目标：`cname.vercel-dns.com`
4. 保存

---

### 步骤 3：等待生效

#### 3.1 DNS 生效时间
- **最快**：5-10 分钟
- **通常**：10-30 分钟
- **最长**：24-48 小时

#### 3.2 检查是否生效

**方法 1：在 Vercel 检查**
- 回到 Vercel Domains 页面
- 看到绿色的 "Valid Configuration" 就成功了

**方法 2：使用命令行检查**
```bash
# Windows
nslookup oc.yourdomain.com

# 应该看到指向 cname.vercel-dns.com
```

**方法 3：直接访问**
- 在浏览器输入：`https://oc.yourdomain.com`
- 如果能访问就成功了

---

## 🎯 推荐配置

### 推荐方案：使用 Cloudflare

**为什么推荐 Cloudflare？**
1. ✅ **免费 CDN**：全球加速，国内访问更快
2. ✅ **自动 HTTPS**：免费 SSL 证书
3. ✅ **DDoS 防护**：保护网站安全
4. ✅ **缓存优化**：提升访问速度
5. ✅ **简单易用**：界面友好

**如何使用 Cloudflare？**

#### 1. 注册 Cloudflare
- 访问：https://www.cloudflare.com
- 注册账号（免费）

#### 2. 添加网站
- 点击 "Add a Site"
- 输入你的域名
- 选择 "Free" 计划

#### 3. 修改 DNS 服务器
Cloudflare 会给你两个 DNS 服务器地址，例如：
```
ns1.cloudflare.com
ns2.cloudflare.com
```

在你的域名注册商处修改 DNS 服务器为这两个地址。

#### 4. 添加 DNS 记录
按照上面 "方案 C" 的步骤添加 CNAME 记录。

#### 5. 等待生效
通常 10-30 分钟即可生效。

---

## 🔧 常见问题

### Q1: 域名需要备案吗？
**A**: 
- 如果使用国内服务器：**需要备案**
- 如果使用 Vercel（海外）：**不需要备案**
- 但不备案可能在国内访问不稳定

### Q2: 可以使用主域名吗？
**A**: 可以，但推荐使用子域名
- ✅ 推荐：`oc.yourdomain.com`
- ⚠️ 可以：`yourdomain.com`

**原因：**
- 主域名可能有其他用途
- 子域名更灵活

### Q3: 绑定后还需要代理吗？
**A**: 
- 如果只绑定域名：**可能还需要**
- 如果使用 Cloudflare：**不需要**（推荐）

### Q4: HTTPS 证书怎么办？
**A**: 
- Vercel 自动提供免费 SSL 证书
- 绑定域名后自动配置
- 无需手动操作

### Q5: 多久能访问？
**A**: 
- DNS 生效：10-30 分钟
- SSL 证书：额外 5-10 分钟
- 总计：15-40 分钟

### Q6: 绑定失败怎么办？
**A**: 检查：
1. DNS 记录是否正确
2. 记录类型是否为 CNAME
3. 记录值是否为 `cname.vercel-dns.com`
4. 是否等待足够时间

---

## 📝 完整示例

### 示例：绑定 `oc.example.com`

#### 1. 在 Vercel 添加
```
Domain: oc.example.com
```

#### 2. 在 DNS 添加记录
```
类型: CNAME
名称: oc
值: cname.vercel-dns.com
TTL: 自动/600
```

#### 3. 等待生效
```
10-30 分钟后访问：
https://oc.example.com
```

---

## 🎨 使用 Cloudflare 的完整流程

### 步骤 1：注册 Cloudflare
1. 访问 https://www.cloudflare.com
2. 点击 "Sign Up"
3. 输入邮箱和密码
4. 验证邮箱

### 步骤 2：添加域名
1. 点击 "Add a Site"
2. 输入你的域名（如 `example.com`）
3. 点击 "Add site"
4. 选择 "Free" 计划
5. 点击 "Continue"

### 步骤 3：导入 DNS 记录
1. Cloudflare 会自动扫描现有 DNS 记录
2. 检查记录是否正确
3. 点击 "Continue"

### 步骤 4：修改 DNS 服务器
1. Cloudflare 显示两个 DNS 服务器地址
2. 复制这两个地址
3. 去你的域名注册商（阿里云/腾讯云等）
4. 修改 DNS 服务器为 Cloudflare 的地址
5. 保存

### 步骤 5：添加 CNAME 记录
1. 在 Cloudflare 点击 "DNS"
2. 点击 "Add record"
3. 填写：
   - Type: `CNAME`
   - Name: `oc`
   - Target: `cname.vercel-dns.com`
   - Proxy status: `Proxied`（橙色云朵）
4. 点击 "Save"

### 步骤 6：等待生效
1. DNS 服务器切换：2-24 小时
2. DNS 记录生效：5-10 分钟
3. 访问测试：`https://oc.example.com`

---

## ✅ 验证成功

### 成功标志
1. ✅ Vercel 显示 "Valid Configuration"
2. ✅ 浏览器能访问 `https://你的域名`
3. ✅ 显示绿色小锁（HTTPS）
4. ✅ 网站正常显示

### 测试功能
- [ ] 随机生成角色
- [ ] 选择风格
- [ ] 导出图片
- [ ] 保存历史记录

---

## 🎉 完成！

绑定成功后：
1. ✅ 国内可以直接访问
2. ✅ 不需要代理
3. ✅ 访问速度更快
4. ✅ 更专业的网址

---

## 💡 提示

1. **推荐使用 Cloudflare**
   - 免费且功能强大
   - 国内访问速度好
   - 自动优化

2. **子域名更灵活**
   - 可以创建多个子域名
   - 不影响主域名使用

3. **耐心等待**
   - DNS 生效需要时间
   - 不要频繁修改

4. **保留 Vercel 域名**
   - 作为备用访问地址
   - 方便调试

---

**祝你绑定成功！** 🚀

如有问题，随时询问！
