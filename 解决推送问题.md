# 🔧 解决 Git 推送问题

## ✅ 好消息
代码已经成功提交到本地仓库！
- 109 个文件已更改
- 25544 行新增代码
- Commit ID: 07726fe

## ❌ 问题
推送到 GitHub 时遇到网络连接问题：
```
fatal: unable to access 'https://github.com/qizhihenying/oc-character-generator.git/': 
Recv failure: Connection was reset
```

---

## 🔧 解决方案

### 方案一：重试推送（最简单）

网络问题通常是暂时的，直接重试：

```bash
git push origin main
```

如果还是失败，尝试下面的方案。

---

### 方案二：使用代理（如果你有代理）

如果你使用了代理软件（如 VPN、Clash 等）：

```bash
# 设置 HTTP 代理（替换为你的代理端口）
git config --global http.proxy http://127.0.0.1:7890
git config --global https.proxy http://127.0.0.1:7890

# 然后重试推送
git push origin main

# 推送成功后可以取消代理设置
git config --global --unset http.proxy
git config --global --unset https.proxy
```

**常见代理端口：**
- Clash: 7890
- V2Ray: 10809
- SSR: 1080

---

### 方案三：使用 SSH 方式推送

HTTPS 方式可能被墙，改用 SSH：

#### 1. 生成 SSH 密钥（如果还没有）
```bash
ssh-keygen -t rsa -b 4096 -C "qizhihenying@gmail.com"
# 一路回车即可
```

#### 2. 复制公钥
```bash
# 查看公钥
cat ~/.ssh/id_rsa.pub
# 或者在 Windows 上
type %USERPROFILE%\.ssh\id_rsa.pub
```

#### 3. 添加到 GitHub
1. 访问 https://github.com/settings/keys
2. 点击 "New SSH key"
3. 粘贴公钥内容
4. 保存

#### 4. 修改远程仓库地址
```bash
# 查看当前远程地址
git remote -v

# 修改为 SSH 地址
git remote set-url origin git@github.com:qizhihenying/oc-character-generator.git

# 推送
git push origin main
```

---

### 方案四：增加超时时间

```bash
# 增加 HTTP 超时时间
git config --global http.postBuffer 524288000
git config --global http.lowSpeedLimit 0
git config --global http.lowSpeedTime 999999

# 重试推送
git push origin main
```

---

### 方案五：分批推送（如果文件太大）

```bash
# 查看提交历史
git log --oneline -5

# 如果有多个提交，可以分批推送
git push origin HEAD~1:main  # 先推送前面的提交
git push origin main          # 再推送最新的
```

---

## 🎯 推荐流程

### 1. 先尝试直接重试
```bash
git push origin main
```

### 2. 如果有代理，配置代理
```bash
# 找到你的代理端口（通常在代理软件设置中）
git config --global http.proxy http://127.0.0.1:7890
git config --global https.proxy http://127.0.0.1:7890
git push origin main
```

### 3. 如果还是失败，使用 SSH
```bash
# 生成密钥
ssh-keygen -t rsa -b 4096 -C "qizhihenying@gmail.com"

# 添加到 GitHub（复制 ~/.ssh/id_rsa.pub 的内容）

# 修改远程地址
git remote set-url origin git@github.com:qizhihenying/oc-character-generator.git

# 推送
git push origin main
```

---

## 📱 检查网络

### 测试 GitHub 连接
```bash
# 测试 HTTPS
curl https://github.com

# 测试 SSH（如果配置了）
ssh -T git@github.com
```

### 检查代理设置
```bash
# 查看当前代理
git config --global --get http.proxy
git config --global --get https.proxy
```

---

## 🆘 如果所有方法都失败

### 临时方案：使用 GitHub Desktop
1. 下载安装 GitHub Desktop
2. 打开项目文件夹
3. 使用图形界面推送

### 或者：使用 Gitee 镜像
1. 在 Gitee 创建仓库
2. 添加 Gitee 远程地址
3. 推送到 Gitee
4. 使用 Gitee 的导入功能同步到 GitHub

---

## ✅ 推送成功后

### 验证
访问你的 GitHub 仓库：
```
https://github.com/qizhihenying/oc-character-generator
```

### 下一步
1. 确认代码已上传
2. 访问 Vercel 部署
3. 配置环境变量
4. 开始使用！

---

## 💡 提示

1. **网络问题很常见**
   - GitHub 在国内访问不稳定
   - 建议使用代理或 SSH

2. **代码已经安全保存**
   - 已提交到本地仓库
   - 不会丢失
   - 可以随时重试推送

3. **推送可能需要时间**
   - 109 个文件，25000+ 行代码
   - 首次推送较慢是正常的
   - 耐心等待

---

**现在就试试重新推送吧！** 🚀

```bash
# 最简单的方式
git push origin main
```
