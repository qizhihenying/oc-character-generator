# 🎴 卡片下载功能 - 快速开始

## ✅ 已完成的集成

卡片下载功能已经成功集成到现有的 `CharacterCard` 组件中！

### 📦 第一步：安装依赖

在项目根目录运行：

```bash
npm install
```

这会自动安装新增的依赖：
- `html2canvas`: ^1.4.1
- `@types/html2canvas`: ^1.0.0

### 🚀 第二步：启动项目

安装完成后，启动开发服务器：

```bash
npm run dev
```

或者启动 Electron 桌面版：

```bash
npm run electron:dev
```

### 🎨 功能说明

现在在每个角色卡片上，你会看到**三个按钮**：

1. **入库** (绿色) - 保存角色到收藏库
2. **下载卡片** (蓝色) ⭐ **新功能！**
3. **复制提示词** (紫色) - 复制完整提示词

### 📸 使用方法

1. **生成角色**：点击"抽取人设盲盒"或连抽按钮
2. **查看卡片**：角色信息会显示在卡片中
3. **下载卡片**：点击"下载卡片"按钮
4. **等待生成**：按钮会显示"生成中..."
5. **自动保存**：卡片会自动保存为PNG图片到下载文件夹

### 📁 文件命名

下载的文件名格式：
```
角色名_生成时间.png
```

例如：
```
银月守护者_14-30-25.png
幻影法师_15-42-18.png
```

### 🎯 卡片内容

下载的图片包含：
- ✅ 生成时间
- ✅ 完整提示词
- ✅ 技术参数（如果有）
- ✅ 角色种子（如果有）
- ✅ 所有元素分类（发型、眼睛、服装等）

### 💡 特性

- **高清晰度**：2倍分辨率渲染（scale: 2）
- **自动命名**：根据角色名和时间自动生成文件名
- **状态提示**：下载过程中显示"生成中..."
- **成功提示**：下载完成后弹出"✓ 卡片已保存！"
- **错误处理**：如果失败会提示重试

### 🔧 技术细节

#### 实现原理
1. 使用 `html2canvas` 将整个卡片DOM转换为Canvas
2. Canvas转换为Blob对象
3. 创建下载链接并触发下载
4. 清理临时资源

#### 关键代码
```typescript
// 将卡片转换为图片
const canvas = await html2canvas(cardRef.current, {
  backgroundColor: '#ffffff',
  scale: 2,              // 2倍清晰度
  useCORS: true,         // 支持跨域图片
  logging: false,        // 关闭日志
});

// 下载图片
canvas.toBlob((blob) => {
  const url = URL.createObjectURL(blob);
  const link = document.createElement('a');
  link.href = url;
  link.download = fileName;
  link.click();
}, 'image/png');
```

### 🎨 自定义选项

如果你想修改下载的图片质量或格式：

#### 提高清晰度
在 `CharacterCard.tsx` 中修改：
```typescript
scale: 3,  // 改为3倍或更高（文件会更大）
```

#### 修改背景色
```typescript
backgroundColor: '#f0f0f0',  // 改为其他颜色
```

#### 保存为JPG格式
```typescript
canvas.toBlob((blob) => {
  // ...
}, 'image/jpeg', 0.95);  // JPEG格式，95%质量
```

### 📱 移动端支持

功能在移动端也完全可用！点击"下载卡片"按钮，图片会保存到手机的下载文件夹。

### 🐛 常见问题

#### Q: 点击下载没反应？
A: 检查浏览器控制台是否有错误。确保已运行 `npm install`。

#### Q: 图片模糊？
A: 增加 `scale` 参数值（如改为3或4）。

#### Q: 下载速度慢？
A: 这是正常的，html2canvas需要时间渲染。通常1-3秒。

#### Q: TypeScript 错误？
A: 运行 `npm install` 后重启 IDE/编辑器。

#### Q: 暗色模式下载的卡片是黑色的？
A: 已设置 `backgroundColor: '#ffffff'`，下载的卡片会是白色背景。

### 🎉 完成！

现在你可以：
1. ✅ 生成角色
2. ✅ 查看精美卡片
3. ✅ 一键下载保存
4. ✅ 分享给朋友

### 🚀 下一步计划

可以考虑添加的功能：
- [ ] 批量下载（连抽结果一键全部下载）
- [ ] 自定义卡片模板
- [ ] 添加水印或签名
- [ ] 分享到社交媒体
- [ ] 卡片编辑器（修改样式）

### 📞 需要帮助？

如果遇到问题：
1. 检查是否运行了 `npm install`
2. 查看浏览器控制台错误信息
3. 确认 `html2canvas` 已正确安装
4. 重启开发服务器

---

**提示**：首次使用请务必先运行 `npm install` 安装依赖！

🎨 享受创作的乐趣！
