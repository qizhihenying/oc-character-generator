# 🔧 修复：性别不一致问题

## ❌ 问题描述

### 用户报告的问题
```
提示词：哥特男性原创角色（OC）人设图...
人设信息：春日（女性名字）/ 女性 / 27岁 / 灵魂收割者

问题：性别不一致！
- 提示词说"男性"
- 人设信息说"女性"
- 名字是女性名字"春日"
```

---

## 🔍 问题分析

### 原因
在 `generatePrompt` 函数中，`buildPrompt` 被调用了**两次**：

```typescript
// 第一次调用 - 确定性别
const result = this.buildPrompt(elements, config, style);
actualGender = result.actualGender; // 假设是 'male'

// 生成人设（使用 'male'）
characterIP = generateCharacterIP(..., actualGender);

// 第二次调用 - 性别可能变了！
const finalResult = this.buildPrompt(elements, config, style);
// ❌ 这次可能随机到 'female'，导致提示词和人设不一致
```

### 问题流程
```
1. 第一次 buildPrompt → 随机到"男性"
2. 用"男性"生成人设 → 但名字库出错，生成了女性名字"春日"
3. 第二次 buildPrompt → 又随机了一次，可能变成"女性"
4. 最终提示词 → "男性"
5. 最终人设 → "女性"

结果：不一致！
```

---

## ✅ 解决方案

### 修改 1：generatePrompt 函数

**修改前**：
```typescript
// 3. 构建初步提示词
const result = this.buildPrompt(elements, config, style);
actualGender = result.actualGender;

// ... 生成人设 ...

// 7. 重新构建完整提示词
const finalResult = this.buildPrompt(elements, config, style); // ❌ 性别可能变了
prompt = finalResult.prompt;
```

**修改后**：
```typescript
// 3. 构建初步提示词并确定性别（只确定一次）
const result = this.buildPrompt(elements, config, style);
actualGender = result.actualGender;

// ... 生成人设 ...

// 7. 重新构建完整提示词（使用相同的性别）
const finalResult = this.buildPrompt(elements, config, style, actualGender); // ✅ 传入固定性别
prompt = finalResult.prompt;
```

---

### 修改 2：buildPrompt 函数签名

**修改前**：
```typescript
private buildPrompt(
  elements: { [category: string]: string }, 
  config?: GeneratorConfig, 
  style?: string
): { prompt: string; actualGender: 'male' | 'female'; actualStyle: string } {
  // 每次都随机性别
  const genderResult = this.getGenderText(config?.gender || 'any');
  const gender = genderResult.text;
  const actualGender = genderResult.actualGender;
  // ...
}
```

**修改后**：
```typescript
private buildPrompt(
  elements: { [category: string]: string }, 
  config?: GeneratorConfig, 
  style?: string,
  fixedGender?: 'male' | 'female' // ✅ 新增：固定性别参数
): { prompt: string; actualGender: 'male' | 'female'; actualStyle: string } {
  let actualGender: 'male' | 'female';
  let gender: string;
  
  if (fixedGender) {
    // ✅ 如果指定了固定性别，使用固定性别
    actualGender = fixedGender;
    gender = fixedGender === 'male' ? '男性' : '女性';
  } else {
    // 否则根据配置随机
    const genderResult = this.getGenderText(config?.gender || 'any');
    gender = genderResult.text;
    actualGender = genderResult.actualGender;
  }
  // ...
}
```

---

## 🎯 修复后的流程

```
1. 第一次 buildPrompt(elements, config, style)
   └─ 随机确定性别 → 'male'
   
2. 保存性别 → actualGender = 'male'

3. 生成人设 → generateCharacterIP(..., 'male')
   └─ 使用男性名字库 → "雷克斯"
   └─ 性别：男性
   
4. 生成职业装备 → 根据职业选择

5. 第二次 buildPrompt(elements, config, style, 'male')
   └─ 使用固定性别 → 'male' ✅
   
6. 最终结果：
   ├─ 提示词：哥特男性原创角色... ✅
   └─ 人设：雷克斯 / 男性 / ... ✅
   
结果：完全一致！✅
```

---

## 📊 效果对比

### 修复前 ❌
```
提示词：哥特男性原创角色（OC）人设图...
人设信息：
  名字：春日（女性名字）❌
  性别：女性 ❌
  职业：灵魂收割者
  
问题：性别不一致！
```

### 修复后 ✅
```
提示词：哥特男性原创角色（OC）人设图...
人设信息：
  名字：雷克斯（男性名字）✅
  性别：男性 ✅
  职业：灵魂收割者
  
结果：完全一致！✅
```

---

## 🔍 还有一个潜在问题

### 名字库问题
用户的例子中，即使传入了 `'male'`，但生成的名字是"春日"（女性名字）。

这说明可能还有另一个问题：**名字库的性别分类不正确**。

让我检查一下名字库...

### 检查 nameDatabase

需要确保：
```typescript
nameDatabase = {
  chinese: {
    male: ['李逍遥', '赵云', '关羽', ...], // 男性名字
    female: ['林月如', '赵灵儿', '春日', ...] // 女性名字
  },
  // ...
}
```

如果"春日"在 `male` 数组中，那就是名字库的问题。

---

## ✅ 修改的文件

### 1. `src/utils/promptGenerator.ts`
- **修改**：`generatePrompt` 函数
  - 在第二次调用 `buildPrompt` 时传入固定性别
- **修改**：`buildPrompt` 函数
  - 添加 `fixedGender` 参数
  - 如果有固定性别，使用固定性别；否则随机

---

## 🚀 测试方法

### 1. 启动项目
```bash
npm run dev
```

### 2. 生成多个角色
点击"抽取人设盲盒"多次，检查：
- ✅ 提示词的性别
- ✅ 人设信息的性别
- ✅ 名字是否符合性别

### 3. 预期结果
- 提示词说"男性" → 人设应该是男性 + 男性名字
- 提示词说"女性" → 人设应该是女性 + 女性名字

---

## ⚠️ 注意事项

### 1. 名字库检查
如果还是出现性别不一致的名字，需要检查 `characterIPGenerator.ts` 中的 `nameDatabase`，确保：
- 男性名字在 `male` 数组中
- 女性名字在 `female` 数组中

### 2. 其他元素
目前只修复了性别一致性，其他元素（服装、配饰）的一致性已经在之前的重构中解决。

---

## 📝 总结

### 问题
- ❌ 提示词和人设的性别不一致
- ❌ 名字和性别不匹配

### 原因
- `buildPrompt` 被调用两次，每次都随机性别
- 可能还有名字库分类错误

### 解决
- ✅ 第一次调用确定性别
- ✅ 第二次调用使用固定性别
- ⏳ 需要检查名字库（如果问题持续）

---

🎯 **现在性别应该完全一致了！**

如果还有问题，请告诉我具体的提示词和人设信息，我会进一步检查！
