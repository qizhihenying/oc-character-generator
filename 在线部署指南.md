# 🌐 OC人设盲盒 - 在线部署指南

## 📋 目录
1. [快速部署方案](#快速部署方案)
2. [详细部署步骤](#详细部署步骤)
3. [部署平台对比](#部署平台对比)
4. [常见问题](#常见问题)

---

## 🚀 快速部署方案

### 方案一：Vercel（推荐 ⭐⭐⭐⭐⭐）
**优点**：免费、快速、自动部署、支持自定义域名
**适合**：个人项目、快速上线

### 方案二：Netlify（推荐 ⭐⭐⭐⭐⭐）
**优点**：免费、简单、功能强大
**适合**：个人项目、团队协作

### 方案三：GitHub Pages（推荐 ⭐⭐⭐⭐）
**优点**：完全免费、与GitHub集成
**适合**：开源项目、个人展示

### 方案四：Cloudflare Pages（推荐 ⭐⭐⭐⭐⭐）
**优点**：免费、速度快、全球CDN
**适合**：需要高性能的项目

---

## 📦 方案一：Vercel 部署（最推荐）

### 步骤 1：准备代码
```bash
# 确保代码已提交到 Git
git add .
git commit -m "准备部署到Vercel"
git push
```

### 步骤 2：注册 Vercel
1. 访问 https://vercel.com
2. 使用 GitHub 账号登录
3. 授权 Vercel 访问你的仓库

### 步骤 3：导入项目
1. 点击 "New Project"
2. 选择你的 GitHub 仓库 `oc-character-generator`
3. Vercel 会自动检测到 Vite 项目

### 步骤 4：配置项目
```
Framework Preset: Vite
Build Command: npm run build
Output Directory: dist
Install Command: npm install
```

### 步骤 5：部署
1. 点击 "Deploy"
2. 等待 1-2 分钟
3. 部署完成！获得在线地址

### 步骤 6：自定义域名（可选）
1. 在 Vercel 项目设置中
2. 添加自定义域名
3. 配置 DNS 记录

**部署后地址示例**：
- `https://oc-character-generator.vercel.app`
- 或自定义域名：`https://your-domain.com`

---

## 📦 方案二：Netlify 部署

### 步骤 1：注册 Netlify
1. 访问 https://netlify.com
2. 使用 GitHub 账号登录

### 步骤 2：新建站点
1. 点击 "Add new site" → "Import an existing project"
2. 选择 GitHub
3. 选择 `oc-character-generator` 仓库

### 步骤 3：配置构建设置
```
Build command: npm run build
Publish directory: dist
```

### 步骤 4：部署
1. 点击 "Deploy site"
2. 等待构建完成
3. 获得在线地址

**部署后地址示例**：
- `https://oc-character-generator.netlify.app`

---

## 📦 方案三：GitHub Pages 部署

### 步骤 1：修改 vite.config.ts
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  base: '/oc-character-generator/', // 改为你的仓库名
  server: {
    port: 5173,
    open: false
  },
  build: {
    outDir: 'dist',
    emptyOutDir: true,
    rollupOptions: {
      output: {
        manualChunks: undefined
      }
    }
  }
})
```

### 步骤 2：添加部署脚本到 package.json
```json
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.0"
  }
}
```

### 步骤 3：安装 gh-pages
```bash
npm install --save-dev gh-pages
```

### 步骤 4：部署
```bash
npm run deploy
```

### 步骤 5：配置 GitHub Pages
1. 进入 GitHub 仓库设置
2. 找到 "Pages" 选项
3. Source 选择 "gh-pages" 分支
4. 保存

**部署后地址**：
- `https://你的用户名.github.io/oc-character-generator/`

---

## 📦 方案四：Cloudflare Pages 部署

### 步骤 1：注册 Cloudflare
1. 访问 https://pages.cloudflare.com
2. 使用 GitHub 账号登录

### 步骤 2：创建项目
1. 点击 "Create a project"
2. 连接 GitHub 仓库
3. 选择 `oc-character-generator`

### 步骤 3：配置构建
```
Framework preset: None
Build command: npm run build
Build output directory: dist
```

### 步骤 4：部署
1. 点击 "Save and Deploy"
2. 等待构建完成

**部署后地址**：
- `https://oc-character-generator.pages.dev`

---

## 🔧 本地构建测试

在部署前，建议先本地测试：

```bash
# 1. 构建项目
npm run build

# 2. 预览构建结果
npm run preview

# 3. 访问 http://localhost:4173 测试
```

---

## 📊 部署平台对比

| 平台 | 免费额度 | 速度 | 自动部署 | 自定义域名 | 难度 | 推荐度 |
|------|----------|------|----------|------------|------|--------|
| **Vercel** | 无限 | ⭐⭐⭐⭐⭐ | ✅ | ✅ | 简单 | ⭐⭐⭐⭐⭐ |
| **Netlify** | 100GB/月 | ⭐⭐⭐⭐⭐ | ✅ | ✅ | 简单 | ⭐⭐⭐⭐⭐ |
| **GitHub Pages** | 1GB | ⭐⭐⭐⭐ | ✅ | ✅ | 中等 | ⭐⭐⭐⭐ |
| **Cloudflare Pages** | 无限 | ⭐⭐⭐⭐⭐ | ✅ | ✅ | 简单 | ⭐⭐⭐⭐⭐ |

---

## 🎯 推荐方案

### 个人使用（最简单）
**推荐：Vercel**
- 一键部署
- 自动更新
- 免费额度充足

### 团队协作
**推荐：Netlify**
- 团队管理功能强大
- 支持多人协作
- 预览部署功能

### 开源项目
**推荐：GitHub Pages**
- 与 GitHub 深度集成
- 完全免费
- 适合开源展示

### 追求极致性能
**推荐：Cloudflare Pages**
- 全球 CDN 加速
- 速度最快
- 免费额度无限

---

## 🚨 重要注意事项

### 1. AI 服务配置
由于你的项目使用了 AI 服务（DeepSeek API），需要注意：

#### 方案 A：前端直接调用（不推荐）
- ⚠️ API Key 会暴露在前端代码中
- ⚠️ 存在安全风险

#### 方案 B：添加后端服务（推荐）
需要创建一个简单的后端 API 来保护你的 API Key：

**选项 1：Vercel Serverless Functions**
```typescript
// api/chat.ts
import type { VercelRequest, VercelResponse } from '@vercel/node';

export default async function handler(
  req: VercelRequest,
  res: VercelResponse
) {
  const apiKey = process.env.DEEPSEEK_API_KEY; // 在 Vercel 环境变量中设置
  
  const response = await fetch('https://api.deepseek.com/v1/chat/completions', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${apiKey}`
    },
    body: JSON.stringify(req.body)
  });
  
  const data = await response.json();
  res.json(data);
}
```

**选项 2：Netlify Functions**
```typescript
// netlify/functions/chat.ts
import { Handler } from '@netlify/functions';

export const handler: Handler = async (event) => {
  const apiKey = process.env.DEEPSEEK_API_KEY;
  
  const response = await fetch('https://api.deepseek.com/v1/chat/completions', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${apiKey}`
    },
    body: event.body
  });
  
  const data = await response.json();
  
  return {
    statusCode: 200,
    body: JSON.stringify(data)
  };
};
```

### 2. 环境变量配置

在部署平台设置环境变量：

**Vercel**：
1. 项目设置 → Environment Variables
2. 添加 `DEEPSEEK_API_KEY`

**Netlify**：
1. Site settings → Environment variables
2. 添加 `DEEPSEEK_API_KEY`

**GitHub Pages**：
- 不支持环境变量
- 需要使用 GitHub Actions + Secrets

### 3. 数据库配置（如果需要）

如果你需要保存用户数据，可以使用：
- **Vercel KV**（Redis）
- **Supabase**（PostgreSQL）
- **Firebase**（NoSQL）
- **MongoDB Atlas**（MongoDB）

---

## 🔒 安全建议

### 1. API Key 保护
```typescript
// ❌ 不要这样做
const apiKey = 'sk-xxxxxxxxxxxxx'; // 直接写在代码里

// ✅ 应该这样做
const apiKey = import.meta.env.VITE_API_KEY; // 使用环境变量
```

### 2. 环境变量配置
创建 `.env` 文件（不要提交到 Git）：
```env
VITE_API_KEY=your_api_key_here
VITE_API_URL=https://api.deepseek.com
```

添加到 `.gitignore`：
```
.env
.env.local
.env.production
```

### 3. 使用后端代理
修改前端代码，调用自己的后端 API：
```typescript
// src/utils/aiService.ts
const API_URL = import.meta.env.VITE_API_URL || '/api/chat';

async function callAI(prompt: string) {
  const response = await fetch(API_URL, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({ prompt })
  });
  
  return response.json();
}
```

---

## 📝 部署检查清单

部署前请确认：

- [ ] 代码已提交到 Git
- [ ] 本地构建测试通过（`npm run build`）
- [ ] 本地预览测试通过（`npm run preview`）
- [ ] API Key 已配置为环境变量
- [ ] `.env` 文件已添加到 `.gitignore`
- [ ] 选择了合适的部署平台
- [ ] 配置了正确的构建命令
- [ ] 测试了所有功能是否正常

---

## 🎉 快速开始（推荐流程）

### 最简单的部署方式：

1. **推送代码到 GitHub**
   ```bash
   git add .
   git commit -m "准备部署"
   git push
   ```

2. **访问 Vercel**
   - 打开 https://vercel.com
   - 用 GitHub 登录
   - 点击 "New Project"
   - 选择你的仓库
   - 点击 "Deploy"

3. **等待 1-2 分钟**
   - 自动构建
   - 自动部署
   - 获得在线地址

4. **完成！** 🎉
   - 访问你的在线地址
   - 分享给朋友使用

---

## 🆘 常见问题

### Q1: 部署后页面空白？
**A**: 检查 `vite.config.ts` 中的 `base` 配置是否正确。

### Q2: API 调用失败？
**A**: 检查环境变量是否正确配置，考虑使用后端代理。

### Q3: 构建失败？
**A**: 检查 Node.js 版本，确保依赖安装完整。

### Q4: 如何更新部署？
**A**: 推送代码到 GitHub，Vercel/Netlify 会自动重新部署。

### Q5: 如何回滚版本？
**A**: 在部署平台的控制面板中选择历史版本回滚。

---

## 📞 需要帮助？

如果遇到问题，可以：
1. 查看部署平台的文档
2. 检查构建日志
3. 在 GitHub Issues 中提问

---

**祝你部署顺利！** 🚀
