# 🎮 游戏风格角色卡片 - 使用说明

## 📸 效果预览

类似于你提供的参考图，新的游戏风格卡片包含：

### 视觉设计
- ✨ **华丽背景**：深蓝渐变 + 水墨效果 + 星光点缀
- 🎨 **装饰元素**：花纹边框、星星、光效
- 👤 **角色立绘**：左侧展示MJ生成的角色图片
- 📝 **信息展示**：右侧黑色半透明卡片
- 💫 **艺术字标题**：金色渐变的角色名

### 卡片布局
```
┌─────────────────────────────────────────┐
│  [装饰花纹]              [星光效果]      │
│                                         │
│  ┌────────┐    ┌──────────────────┐   │
│  │        │    │  【角色名】       │   │
│  │  角色  │    │  ═══════════      │   │
│  │  立绘  │    │                   │   │
│  │  图片  │    │  ★ 基础信息       │   │
│  │        │    │  女性/19岁/企业家  │   │
│  │        │    │                   │   │
│  │        │    │  ✨ 性格特征       │   │
│  │        │    │  暴躁/沉默        │   │
│  │        │    │                   │   │
│  │        │    │  ★ 外貌描述       │   │
│  │        │    │  • 翠绿色编发...  │   │
│  │        │    │  • 皮帕色眼瞳...  │   │
│  │        │    │  • 红色中式唐宗   │   │
│  │        │    │                   │   │
│  │        │    │  ✨ 背景设定       │   │
│  │        │    │  不为人知的过去... │   │
│  └────────┘    └──────────────────┘   │
│                                         │
│  [装饰花纹]              [水印]         │
└─────────────────────────────────────────┘
```

## 🚀 使用方法

### 方法1：在 App.tsx 中添加

在生成角色后显示游戏风格卡片：

```typescript
import GameStyleCard from './components/GameStyleCard';

// 在 App.tsx 中，currentPrompt 显示区域添加
{currentPrompt && (
  <>
    {/* 原有的 CharacterCard */}
    <CharacterCard
      prompt={currentPrompt}
      onCopy={handleCopy}
      isCopied={copiedId === currentPrompt.id}
    />
    
    {/* 新增：游戏风格卡片 */}
    <GameStyleCard
      prompt={currentPrompt}
      characterImage={currentPrompt.mjImageUrl} // 如果有MJ生成的图片
    />
  </>
)}
```

### 方法2：作为独立页面

创建一个专门的卡片预览页面：

```typescript
// src/pages/CardPreview.tsx
import GameStyleCard from '../components/GameStyleCard';

function CardPreview() {
  const [currentPrompt, setCurrentPrompt] = useState(null);
  
  return (
    <div className="min-h-screen bg-gray-900 py-8">
      <GameStyleCard 
        prompt={currentPrompt}
        characterImage="/path/to/image.png"
      />
    </div>
  );
}
```

## 🎨 自定义样式

### 修改背景颜色

在 `GameStyleCard.tsx` 中修改：

```typescript
// 主背景渐变
style={{
  background: 'linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%)',
}}

// 改为其他颜色，例如：
// 紫色主题
background: 'linear-gradient(135deg, #2d1b69 0%, #5b2a86 50%, #8e44ad 100%)'

// 红色主题
background: 'linear-gradient(135deg, #4a0e0e 0%, #7a1818 50%, #a82828 100%)'

// 绿色主题
background: 'linear-gradient(135deg, #0e4a2d 0%, #187a4a 50%, #28a86e 100%)'
```

### 修改标题颜色

```typescript
// 角色名称渐变
background: 'linear-gradient(135deg, #FFD700 0%, #FFA500 50%, #FF8C00 100%)'

// 改为其他颜色：
// 银色
background: 'linear-gradient(135deg, #C0C0C0 0%, #E8E8E8 50%, #F5F5F5 100%)'

// 紫色
background: 'linear-gradient(135deg, #9D50BB 0%, #C850C0 50%, #F050F0 100%)'

// 蓝色
background: 'linear-gradient(135deg, #4A90E2 0%, #50C0E8 50%, #50E8F0 100%)'
```

### 修改卡片尺寸

```typescript
// 当前尺寸：800x1200
className="relative w-[800px] h-[1200px]"

// 改为其他尺寸：
// 小卡片
className="relative w-[600px] h-[900px]"

// 大卡片
className="relative w-[1000px] h-[1500px]"

// 横向卡片
className="relative w-[1200px] h-[800px]"
```

## 📸 下载功能

### 使用方法
1. 生成角色
2. 等待MJ绘图完成（可选）
3. 点击"下载游戏风格角色卡"按钮
4. 卡片会保存为 `角色名_角色卡.png`

### 下载设置

```typescript
// 修改清晰度
scale: 2,  // 改为 3 或 4 更清晰

// 修改输出尺寸
width: 800,   // 固定宽度
height: 1200, // 固定高度

// 修改文件名
link.download = `${characterName}_角色卡.png`;
// 改为：
link.download = `OC_${characterName}_${Date.now()}.png`;
```

## 🎯 集成到 MJ 画板

如果想在 MJ 生成图片后自动显示游戏风格卡片：

```typescript
// 在 MJGallery.tsx 中
import GameStyleCard from './GameStyleCard';

// 当任务完成时
{task.status === 'success' && (
  <GameStyleCard
    prompt={task.originalPrompt}
    characterImage={task.imageUrl}
  />
)}
```

## 💡 使用场景

### 1. 角色展示
- 生成角色后立即预览精美卡片
- 适合社交媒体分享

### 2. 角色收藏
- 下载保存喜欢的角色
- 建立个人角色图鉴

### 3. 创作参考
- 打印卡片作为创作参考
- 制作角色设定集

### 4. 游戏素材
- 用于桌游、TRPG
- 角色卡牌设计

## 🔧 高级功能

### 添加稀有度系统

```typescript
// 在 GameStyleCard.tsx 中添加
const rarity = calculateRarity(prompt); // 'N', 'R', 'SR', 'SSR', 'UR'

// 根据稀有度改变边框颜色
const rarityColors = {
  'N': 'border-gray-500',
  'R': 'border-blue-500',
  'SR': 'border-purple-500',
  'SSR': 'border-yellow-500',
  'UR': 'border-pink-500'
};

// 添加稀有度标识
<div className="absolute top-4 right-4">
  <div className={`px-4 py-2 ${rarityColors[rarity]} border-2 rounded-lg`}>
    {rarity}
  </div>
</div>
```

### 添加动画效果

```typescript
// 翻卡动画
<div className="animate-flip-in">
  <GameStyleCard ... />
</div>

// 在 tailwind.config.js 中添加
module.exports = {
  theme: {
    extend: {
      keyframes: {
        'flip-in': {
          '0%': { transform: 'rotateY(90deg)', opacity: '0' },
          '100%': { transform: 'rotateY(0)', opacity: '1' }
        }
      },
      animation: {
        'flip-in': 'flip-in 0.6s ease-out'
      }
    }
  }
}
```

### 批量生成卡片

```typescript
// 连抽结果批量生成
const generateAllCards = async () => {
  for (const prompt of multiDrawResults) {
    await new Promise(resolve => setTimeout(resolve, 1000));
    // 生成并下载每张卡片
  }
};
```

## 🐛 常见问题

### Q: 卡片显示不完整？
A: 检查容器高度，确保有足够空间显示 1200px 高度。

### Q: 图片不显示？
A: 确保 `characterImage` 路径正确，且支持跨域（CORS）。

### Q: 下载的图片模糊？
A: 增加 `scale` 参数值（如改为 3 或 4）。

### Q: 想要横向卡片？
A: 修改 `w-[800px] h-[1200px]` 为 `w-[1200px] h-[800px]`，并调整布局。

### Q: 如何添加更多信息？
A: 在信息卡片区域添加新的 `<div>` 块，参考现有样式。

## 📝 完整示例

```typescript
// App.tsx 完整集成示例
import GameStyleCard from './components/GameStyleCard';

function App() {
  const [currentPrompt, setCurrentPrompt] = useState(null);
  const [mjImageUrl, setMjImageUrl] = useState(null);

  return (
    <div>
      {/* 生成按钮 */}
      <button onClick={handleGenerate}>
        抽取人设盲盒
      </button>

      {/* 显示卡片 */}
      {currentPrompt && (
        <div className="space-y-8">
          {/* 原有卡片 */}
          <CharacterCard prompt={currentPrompt} />
          
          {/* 游戏风格卡片 */}
          <GameStyleCard
            prompt={currentPrompt}
            characterImage={mjImageUrl}
          />
        </div>
      )}
    </div>
  );
}
```

## 🎉 完成！

现在你可以生成精美的游戏风格角色卡片了！

**特点**：
- ✅ 华丽的视觉设计
- ✅ 完整的角色信息
- ✅ 一键下载保存
- ✅ 高度可自定义

**下一步**：
1. 在 App.tsx 中添加组件
2. 生成角色测试效果
3. 根据需要调整样式

🎨 让每个角色都成为艺术品！
