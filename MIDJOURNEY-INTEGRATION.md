# Midjourney API 集成功能 - 使用指南

## 🎨 功能概述

本软件已集成第三方 Midjourney API，可以自动将生成的角色提示词发送到 Midjourney 进行绘图，并在画板中实时显示生成进度和结果。

## ✨ 主要特性

### 1. 自动绘图
- 生成提示词后一键发送到 Midjourney
- 自动轮询任务状态，实时显示进度
- 支持 Midjourney 和 Niji Journey 两种模式

### 2. 垫图参考（新增）
- 上传参考图片（最多 5 张）
- MJ 会根据图片风格生成角色
- 支持拖拽上传和批量选择
- 实时预览已上传的图片

### 3. 画板展示
- 网格布局展示所有绘图任务
- 实时显示任务状态（等待中/生成中/完成/失败）
- 显示生成进度百分比
- 点击图片可全屏查看

### 3. Midjourney 操作
- **U1-U4**：放大指定位置的图片（Upscale）
- **V1-V4**：生成指定位置的变体（Variation）
- **重绘**：重新生成整张图片（Reroll）
- **下载**：保存生成的图片到本地

### 4. 任务管理
- 查看所有历史任务
- 清空所有任务记录
- 任务自动保存在内存中

## 🔧 配置步骤

### 1. 获取 API Key

你需要从第三方 Midjourney API 服务商获取 API Key。常见的服务商包括：
- Midjourney API
- GoAPI
- 其他兼容的 API 服务

### 2. 配置 API

1. **打开配置面板**
   - 点击"AI 绘图"按钮旁边的设置图标⚙️
   - 或点击"配置 API"按钮

2. **填写配置信息**
   ```
   API Key: 你的 API 密钥
   API Base URL: https://api.vectorengine.ai （默认）
   回调地址: （可选）留空即可
   ```

3. **测试连接**
   - 点击"测试连接"按钮
   - 系统会发送测试请求验证配置
   - 显示连接成功或错误信息

4. **保存配置**
   - 确认测试成功后点击"保存配置"
   - 配置会自动保存到本地浏览器

### 3. 开始使用

配置并测试成功后，生成一个提示词并点击"AI 绘图"按钮开始绘图。

## 📖 使用流程

### 基础使用

1. **生成角色提示词**
   - 点击"抽取人设盲盒"生成角色
   - 查看生成的提示词

2. **选择 Bot 类型和选项**
   - 点击"AI 绘图"按钮旁的⚙️图标
   - 选择 Midjourney 或 Niji Journey
   - Midjourney：适合各种风格
   - Niji Journey：专注于动漫风格

3. **上传垫图（可选）**
   - 在选项面板中点击"上传垫图"
   - 选择 1-5 张参考图片
   - 预览已上传的图片
   - 可以删除不需要的图片

4. **提交绘图任务**
   - 点击"AI 绘图"按钮
   - 任务会自动提交到 Midjourney
   - 如果有垫图，会一起发送
   - 在画板中查看任务状态

5. **等待生成完成**
   - 任务会显示"生成中"状态
   - 进度条显示当前进度
   - 通常需要 30-60 秒完成

6. **查看和操作结果**
   - 生成完成后，图片会显示在画板中
   - 使用 U/V/重绘按钮进行进一步操作
   - 点击下载按钮保存图片

### 高级操作

#### 放大（Upscale）- U1-U4
- 点击 U1-U4 按钮放大对应位置的图片
- 生成高分辨率的单张图片
- 适合选择最喜欢的一个进行精细化

```
┌─────┬─────┐
│ U1  │ U2  │
├─────┼─────┤
│ U3  │ U4  │
└─────┴─────┘
```

#### 变体（Variation）- V1-V4
- 点击 V1-V4 按钮生成对应位置的变体
- 基于选中的图片生成新的变化
- 保持风格相似但细节不同

```
┌─────┬─────┐
│ V1  │ V2  │
├─────┼─────┤
│ V3  │ V4  │
└─────┴─────┘
```

#### 重绘（Reroll）
- 点击"重绘"按钮
- 使用相同的提示词重新生成
- 获得完全不同的结果

#### 下载图片
- 点击"下载"按钮
- 图片会保存到浏览器的下载文件夹
- 文件名格式：`mj-{任务ID}.png`

## 🎯 使用技巧

### 1. 提示词优化
- 本软件生成的提示词已针对 Midjourney 优化
- 包含必要的参数（--niji, --style, --ar 等）
- 可以在"自定义许愿"中添加额外要求

### 2. Bot 类型选择
- **Midjourney**：
  - 适合写实、奇幻、科幻等风格
  - 细节更丰富，质量更高
  - 适合复杂场景

- **Niji Journey**：
  - 专注于动漫风格
  - 更符合日系动漫审美
  - 适合二次元角色

### 3. 批量生成
1. 使用连抽功能（3抽/5抽/10抽）
2. 选择喜欢的提示词
3. 点击"AI 绘图"提交任务
4. 在画板中查看所有结果

### 4. 迭代优化
1. 生成初始图片
2. 使用 V1-V4 生成变体
3. 选择最好的变体继续 V
4. 最终使用 U 放大精选图片

## 💰 费用说明

### API 调用费用
- 使用第三方 API 会产生费用
- 费用由 API 服务商收取
- 通常按调用次数计费
- 具体价格请咨询你的 API 服务商

### 费用估算
- Imagine（生成）：约 $0.02-0.05/次
- Upscale（放大）：约 $0.01-0.02/次
- Variation（变体）：约 $0.02-0.05/次
- Reroll（重绘）：约 $0.02-0.05/次

**注意**：以上价格仅供参考，实际费用以服务商为准。

## 🔍 常见问题

### Q: 为什么需要配置 API？
A: Midjourney 官方不提供直接 API，需要通过第三方服务商提供的 API 接口访问。

### Q: API Key 安全吗？
A: API Key 仅保存在你的本地浏览器中，不会上传到任何服务器。但请妥善保管，不要泄露给他人。

### Q: 如何测试 API 配置是否正确？
A: 在配置面板中点击"测试连接"按钮，系统会：
1. 发送测试请求到 API 服务器
2. 验证 API Key 是否有效
3. 检查网络连接是否正常
4. 显示详细的成功或错误信息

### Q: 任务一直显示"生成中"怎么办？
A: 可能的原因：
1. Midjourney 服务器繁忙，需要等待更长时间
2. API 配置错误，使用"测试连接"功能验证
3. 网络连接问题，检查网络状态
4. API 额度不足，联系服务商充值

### Q: 生成失败怎么办？
A: 检查以下几点：
1. 使用"测试连接"验证 API 配置
2. 确认 API Key 是否正确
3. 检查 API 额度是否充足
4. 确认提示词是否符合 Midjourney 规范
5. 检查网络连接是否正常

### Q: 可以使用官方 Midjourney 账号吗？
A: 不可以。本功能需要使用第三方 API 服务，不能直接使用官方账号。

### Q: 生成的图片保存在哪里？
A: 
- 任务记录保存在浏览器内存中
- 下载的图片保存在浏览器的下载文件夹
- 清空浏览器缓存会丢失任务记录

### Q: 如何清空所有任务？
A: 点击画板右上角的"清空全部"按钮。

## 🛡️ 隐私与安全

- **本地存储**：API Key 仅保存在本地浏览器
- **不上传服务器**：配置信息不会上传到我们的服务器
- **直接调用**：提示词直接发送到你配置的 API 服务
- **可随时删除**：可以随时清除本地保存的配置

## 📝 API 接口说明

### 提交 Imagine 任务
```
POST /mj/submit/imagine
```

**请求参数：**
```json
{
  "botType": "MID_JOURNEY",
  "prompt": "角色提示词",
  "base64Array": [],
  "notifyHook": "",
  "state": ""
}
```

**响应：**
```json
{
  "code": 1,
  "description": "Submit success",
  "result": "任务ID"
}
```

### 查询任务状态
```
GET /mj/task/{taskId}/fetch
```

**响应：**
```json
{
  "code": 1,
  "result": {
    "id": "任务ID",
    "status": "SUCCESS",
    "progress": "100",
    "imageUrl": "图片地址"
  }
}
```

### 提交操作（U/V/Reroll）
```
POST /mj/submit/action
```

**请求参数：**
```json
{
  "taskId": "原任务ID",
  "action": "UPSCALE",
  "index": 1
}
```

## 🚀 未来计划

- [x] 支持垫图（Image Reference）✅ 已完成
- [ ] 支持 Blend 混合功能
- [ ] 支持 Describe 图生文
- [ ] 任务历史持久化存储
- [ ] 批量下载功能
- [ ] 自定义参数配置
- [ ] 成本统计功能
- [ ] 批量提交（连抽结果一键绘图）

## 📞 技术支持

如有问题或建议，欢迎反馈！

---

**版本**: v2.2.0  
**更新日期**: 2025-11-22

🎨 让 AI 绘图更简单，让创作更自由！
